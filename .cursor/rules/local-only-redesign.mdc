---
description: 只在本地空白改版上工作，禁止动线上版本
alwaysApply: true
---

# 本地改版约束

本项目后续所有编程工作都从本地空白版开始，禁止擅自动线上版本。

## 允许

- 只在当前本地分支 `codex/redesign` 上改代码
- 本地开发、本地预览（`npm start`，端口 3008）
- 用户明确要求时，才在本地分支提交

## 禁止（除非用户当次明确要求）

- 切换到、修改、合并、推送 `main` / `origin/main`
- `git push` 到任何远程分支
- 部署、发布、改 Cloudflare / GitHub Pages / 任何线上托管配置
- 把本地改版覆盖到线上站点

线上站点对应远程 `origin/main`。本地快照在 `snapshot/local-2026-09-09`。当前改版起点是空白两页脚手架。
